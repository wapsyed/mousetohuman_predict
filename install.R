# R packages needed by RunYourAnalysis_MouseToHuman.Rmd
#
# Delegates to notebook/required.R, which installs anything missing (CRAN and
# Bioconductor) and then loads the packages. Run it from the repository root
# (Binder does this automatically during the build).
source("notebook/required.R")
